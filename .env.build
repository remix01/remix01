# Build-time environment variables
# Used only during Docker image build
# Do NOT expose secrets here - use runtime .env instead

NODE_ENV=production
NEXT_PUBLIC_SUPABASE_URL=https://placeholder.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=placeholder-key
SUPABASE_SERVICE_ROLE_KEY=placeholder-key
QSTASH_CURRENT_SIGNING_KEY=placeholder-key
QSTASH_NEXT_SIGNING_KEY=placeholder-key
QSTASH_TOKEN=placeholder-token
REDIS_PASSWORD=placeholder-password
